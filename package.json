{
  "name": "vite-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview --port $PORT --host $HOST",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  }
}